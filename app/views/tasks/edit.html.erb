<% content_for :title, "Editing task" %>

<div class="container my-4" style="max-width: 600px;">
  <div class="card shadow-sm">
    <div class="card-body">
      <h1 class="card-title mb-3"><%= t('.title') %></h1>
      <%= render "form", task: @task %>
      <div class="mt-3">
        <%= link_to t('.back'), tasks_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>
